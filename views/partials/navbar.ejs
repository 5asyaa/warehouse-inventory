<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="/dashboard/<%= user.role.toLowerCase() %>">
            <i class="bi bi-box-seam me-2"></i>Warehouse
        </a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarOffcanvas">
            <i class="bi bi-list text-white fs-4"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <% if (user.role === 'User') { %>
                <ul class="navbar-nav me-auto align-items-center gap-3">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard/user">
                            <i class="bi bi-speedometer2 me-2"></i>Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/peminjaman">
                            <i class="bi bi-clock-history me-2"></i>Riwayat Peminjaman
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/peminjaman/create">
                            <i class="bi bi-plus-circle me-2"></i>Ajukan Peminjaman
                        </a>
                    </li>
                </ul>
            <% } %>
            <ul class="navbar-nav ms-auto align-items-center gap-3">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center gap-3 p-0" href="#" role="button" data-bs-toggle="dropdown">
                        <div class="user-avatar">
                            <%= user.namaLengkap.charAt(0).toUpperCase() %>
                        </div>
                        <div class="text-end d-none d-md-block">
                            <div class="fw-semibold text-white"><%= user.namaLengkap %></div>
                            <span class="role-badge <%= user.role.toLowerCase() %>"><%= user.role %></span>
                        </div>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end border-0 shadow-lg mt-2" style="background-color: var(--bg-tertiary);">
                        <li><a class="dropdown-item text-white" href="/auth/profile"><i class="bi bi-person me-2"></i>Profile</a></li>
                        <li><a class="dropdown-item text-white" href="/auth/change-password"><i class="bi bi-key me-2"></i>Ganti Password</a></li>
                        <li><hr class="dropdown-divider" style="border-color: var(--border-color);"></li>
                        <li><a class="dropdown-item text-danger" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Mobile Sidebar Offcanvas -->
<div class="offcanvas offcanvas-start" tabindex="-1" id="sidebarOffcanvas" style="background-color: var(--bg-secondary);">
    <div class="offcanvas-header border-bottom" style="border-color: var(--border-color);">
        <h5 class="offcanvas-title text-white">Menu</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body p-0">
        <% if (user.role === 'Admin') { %>
            <%- include('../partials/sidebar') %>
        <% } else { %>
            <div class="sidebar">
                <div class="sidebar-header">
                    <h6>Menu Utama</h6>
                </div>
                <a class="nav-link" href="/dashboard/user">
                    <i class="bi bi-speedometer2"></i>
                    <span>Dashboard</span>
                </a>
                <a class="nav-link" href="/peminjaman">
                    <i class="bi bi-clock-history"></i>
                    <span>Riwayat Peminjaman</span>
                </a>
                <a class="nav-link" href="/peminjaman/create">
                    <i class="bi bi-plus-circle"></i>
                    <span>Ajukan Peminjaman</span>
                </a>
            </div>
        <% } %>
    </div>
</div>
